import { ForIterateToCountPipe } from './for-iterate-to-count.pipe';

describe('ForIterateToCountPipe', () => {
  it('create an instance', () => {
    const pipe = new ForIterateToCountPipe();
    expect(pipe).toBeTruthy();
  });
});
